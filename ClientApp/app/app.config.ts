import { Http, Headers, URLSearchParams } from '@angular/http';

export const WebApi = ".."; 
export const WebUrl = ".."; 
export const WebApiRoot = ".."; 
export const Header = new Headers({ 'Content-Type': 'application/json' });